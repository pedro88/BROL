# GSD context snapshot (2026-05-11T08:40:05.783Z)

## Top project memories
- [MEM005] (gotcha) tRPC v11.17.0 + React 19 + React Query v5: `trpc.X.useQuery()` works in browser but E2E tests see 0 tRPC HTTP requests when using Playwright's `page.on("request")`. Root cause: tRPC React hooks work correctly in real browser but Playwright's request interceptor misses requests under specific conditions (possibly network timing). Workaround: use `page.waitForLoadState("networkidle")` or `waitForTimeout()` after navigation in tests — this allows the request to complete before assertions.
- [MEM006] (gotcha) E2E test score baseline: 60/76 (79%) was stable across all attempts. After removing Playwright webServer config and all test fixes, still 60/76. The 16 failures are: isPublic toggle (1), collection detail (2), collection card navigation (1), collection create dialog (2), objects add (3), object detail (1), empty state (1), navigation (1), public access (2), private access (2). These are real UI behavior issues, not tRPC infrastructure failures.
- [MEM007] (gotcha) `better-auth` session cookie: BetterAuth stores the session token as a cookie named "better-auth.session_token" with value "<sessionId>.<signature>" (URL-encoded). The raw token returned by `sign-up`/`sign-in` API is the sessionId. To get the full token (with signature) needed for cookie injection, call `/api/auth/get-session` with the raw token as Bearer Authorization. The full token is URL-encoded — use as-is without decoding.
- [MEM009] (pattern) tRPC v11 body format: after removing superjson from both client and server, mutations use flat JSON `{ name, isPublic: true }` — no `{ json: { ... } }` wrapper. Superjson was removed because it was causing response parsing issues in the React 19 environment. API responses are now plain `{ result: { data: { items: [...] } } }`.
- [MEM008] (gotcha) E2E auth flow: `signInEmailPassword()` in `auth-client.ts` calls BetterAuth's `/api/auth/sign-in/email` and returns `{ session, user }` on success (n
…[truncated]
