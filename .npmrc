# Expo + React Native ne supportent pas le store isolé de pnpm
# (trop de dépendances fantômes dans la chaîne expo/metro/react-native).
# On force un node_modules à plat, recommandé par Expo pour les monorepos pnpm.
# Réf : https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
